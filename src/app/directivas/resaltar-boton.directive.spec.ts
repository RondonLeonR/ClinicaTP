import { ResaltarBotonDirective } from './resaltar-boton.directive';

describe('ResaltarBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarBotonDirective();
    expect(directive).toBeTruthy();
  });
});
