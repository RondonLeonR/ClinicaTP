import { CortarPalabrasPipe } from './cortar-palabras.pipe';

describe('CortarPalabrasPipe', () => {
  it('create an instance', () => {
    const pipe = new CortarPalabrasPipe();
    expect(pipe).toBeTruthy();
  });
});
