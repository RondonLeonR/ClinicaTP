import { MouseSobreBotonDirective } from './mouse-sobre-boton.directive';

describe('MouseSobreBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseSobreBotonDirective();
    expect(directive).toBeTruthy();
  });
});
